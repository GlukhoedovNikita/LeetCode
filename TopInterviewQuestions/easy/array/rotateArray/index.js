// const rotate = (arr, value) =>
//     [...arr.slice(arr.length - value).reverse(), ...arr.slice(0, arr.length - value)]

const value = 3
const arr = [1, 2, 3, 4, 5, 6, 7]

console.log(rotate(arr, value))